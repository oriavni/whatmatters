"use client";

/**
 * TODO (Prompt 11): Implement schedule form (frequency, time, timezone).
 */
export function DigestScheduleForm() {
  return (
    <div className="text-sm text-muted-foreground">
      DigestScheduleForm — coming in Prompt 11
    </div>
  );
}
