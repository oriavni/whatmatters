/**
 * buttonVariants — re-exported from @/components/ui/button for use in:
 *   - Server Components (e.g. <Link className={buttonVariants()}>)
 *   - Any file that cannot import a "use client" component directly
 *
 * The canonical definition lives in components/ui/button.tsx.
 */
export { buttonVariants } from "@/components/ui/button";
export type { VariantProps } from "class-variance-authority";
